V9:

1c. A la fin Dark et Light devrait partager les mêmes variables pour tous les éléments, avec seulement des couleurs différentes; donc la partie Style functions (ligne 37) devrait être séparé des styles vers un fichier externe modules/interface.py qui correspondrait à l'interface neutre sans module sélectionné. Chaque module doit amener ses propres paramètres, exemple checkpoint_manager_layout.py comporterait les couleurs neg/pos et les définitions card_border_(green/red)(), checkpoint_label(), def score_label() et def criterion_button_(neutral/green/red)(), le reste concerne l'interface par défaut


2. Options > General : ajouter les options "Titre" et "Description" (décochées par défaut) qui permettent d'éditer la zone Title et Description des cards (l'équivalent de checkpointName et criteria dans les grids JSON du mode Checkpoint Manager).
	Cela induit que par défaut quand aucun module n'est sélectionné l'export de grid renseigne les paramètres absolutePath, title, description

3. Options > General > Langues : la définition des langues ne devrait pas être en dur (exemple self.lang_fr = QCheckBox("Français")), ça rend compliqué l'ajout de nouvelles langues. Le nom de la langue elle-même devrait être dans le fichier langue, par exemple 'lang_name': 'Français'

	
5. Nouveau module "Metadata" : Si activé, un paramètre metadata est ajouté aux JSON, avec les sous-paramètres 'type' et 'meta'.
	> Options du module Metadata (cochées par défaut), Type: "EXIF", "IPTC", "XMP"
	> détection des metadatas à développer plus tard; afficher le contenu

6. Ajouter l'export de la grille d'images en une image unique. Menu> Bouton [ Grid2Img ], avant le bouton Export.
	- ouvre une fenêtre> options de textes à ajouter, dépendant des informations disponibles sur les images, en fonction du module ou du JSON chargé. Par exemple en mode checkpoint Manager on récupère les paramètres absolutePath, checkpointName, criteria, totalScore
	- en gros l'image sera une copie simplifiée de la grille d'images, customisable:
		- titre
		- fond uni (blanc par défaut)
		- espacement des images (10px par défaut)
		
8. Options>General>Langues : Doit être une liste similaire à ce qu'on a fait pour Personnalisation - Available Style, sauf qu'on remplace les icônes par des flags de 24x16 (taille max 24x24) déterminé dans le fichier langue. Par exemple dans config/lang/fr.py on pourra définir 'lang_icon': 'flags/fr.gif', si aucun icône défini alors on laisse un espace vide de 24x24 à la place pour garder l'alignement avec les autres langues. Les flags seront placés dans config\lang\flags.
	MAIS avant cela il va falloir rendre l'ajout de langues plus dynamique, il faut donc refactoriser le code.
9. langues : TOUT doit être traduis (sauf les infos du clic droit, je n'ai pas ajouté card_details_dialog.py dans le projet car il est stable, pas besoin de le changer pour l'instant). Par exemple dans Optionsles noms des onglets, le paramètre "Aucun module", ou dans le menu le "select action", le titre de la fenêtre après avoir cliqué sur la zone de drop etc, cherche les traductions manquantes et corrige